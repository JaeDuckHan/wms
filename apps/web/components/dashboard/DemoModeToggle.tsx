"use client";

export function DemoModeToggle(props: { demoMode: boolean; onToggle: () => void }) {
  void props;
  return null;
}

export function DemoModeBanner(props: { demoMode: boolean }) {
  void props;
  return null;
}
